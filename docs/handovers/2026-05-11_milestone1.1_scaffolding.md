# Handover — 2026-05-11 — Milestone 1.1: Project Scaffolding

**Session Date:** 2026-05-11  
**Session Focus:** Milestone 1.1 — Full monorepo scaffolding (no feature code)  
**Session Type:** Implementation  
**Status:** COMPLETE — Full skeleton in place; next session = Milestone 1.2 (Bank Import Engine)

---

## 1. What Was Done

### Backend (`/backend`)

- FastAPI application factory (`app/main.py`) with CORS and all routers mounted under `/api/v1/`
- Settings management via `pydantic-settings` (`app/config.py`)
- Async SQLAlchemy 2.0 engine + session factory (`app/database.py`)
- All 10 ORM models matching Spec 04 database design exactly:
  - `Company`, `BankAccount`, `ImportBatch`, `RawMovement`, `Movement`
  - `CategoryTaxonomy`, `ClassificationRule`, `MovementClassification`
  - `IntercompanyMatch`, `ForecastScenario`, `ForecastEntry`
  - `DebtInstrument`, `DebtScheduleEntry`
- All enum types defined as Python enums, mirroring Spec 04 PostgreSQL enum types
- Pydantic v2 schemas: `common.py` (PaginatedResponse, ErrorResponse), `company.py`, `movement.py`, `import_batch.py`
- Router stubs: `health`, `companies` (with list + get), `imports`, `movements`, `classifications`, `intercompany`, `analytics`
- Service directory structure (stubs only — all bounded contexts have `__init__.py`):
  - `import_engine/`, `import_engine/parsers/`, `classification/`, `intercompany/`, `forecast/`, `analytics/`, `excel/`
- Auth dependency stub (`dependencies/auth.py`) — returns `"system"` in Phase 1
- Health endpoint (`GET /api/v1/health`) — checks DB connection
- Test infrastructure: `conftest.py` with async test DB, `client` fixture, `test_api/test_health.py`
- `pyproject.toml` + `requirements.txt` with all required dependencies
- `Dockerfile` for Railway deployment
- `.env.example` with all required variables
- `alembic.ini` + `migrations/env.py` (async Alembic) + `migrations/versions/0001_initial_schema.py`

### Initial Migration (`0001_initial_schema`)

Complete initial schema covering all tables from Spec 04:
- All enum types created explicitly before tables
- Partial indexes for `is_internal`, `is_intercompany`, `is_deleted`, `is_active` flags
- Full upgrade() + downgrade() implemented

### Seed Script (`/scripts/seed.py`)

Idempotent seed (skips existing records):
- 6 companies from confirmed company registry (BPO/AUTHOR/SKILLS/FACTORY/ENGAGE/LMS)
- 19 taxonomy entries covering all codes from Spec 08 v1.1 (OCF_*, ICF_*, FCF_*, INT_*, UNCLASSIFIED)
- 9 intercompany classification rules (priority 5-6) from company registry keywords

### Frontend (`/frontend`)

- `package.json` with Next.js 14 (App Router), TypeScript strict mode, TailwindCSS, shadcn-compatible setup
- All required dependencies: TanStack Table v8, React Query v5, Zustand, Recharts, React Hook Form + Zod, Lucide React
- `tsconfig.json` (strict mode, path alias `@/*`)
- `next.config.ts` (standalone output for Railway)
- `tailwind.config.ts` — finance-grade palette (positive/negative/warning colors, Inter + JetBrains Mono fonts)
- `app/globals.css` — CSS variables for shadcn/ui theming + amount utility classes
- Full App Router structure: `dashboard/`, `ledger/`, `import/`, `intercompany/`, `forecast/`, `settings/`
- Root layout with `Providers` wrapper (React Query) and `Sidebar` component
- `Sidebar` — full nav with Lucide icons, active-state highlighting, all 6 routes
- `lib/utils.ts` — `cn()` utility
- `lib/formatters/` — EUR amount formatter + date formatter (ES locale)
- `lib/api/client.ts` — typed fetch wrapper with `ApiError` class
- `lib/api/companies.ts` — companies API client (list + get)
- `store/filters.ts` — Zustand store with date range (default: last 30 days), companyIds, bankAccountIds
- `store/ui.ts` — Zustand store for sidebar + modal state
- `types/api.ts` — full TypeScript interfaces matching backend Pydantic schemas
- `Dockerfile` (multi-stage, standalone Next.js for Railway)
- `.env.example`

### Shared (`/shared`)

- `constants.py` + `constants.ts` — taxonomy codes, company short names, supported banks list

### Infrastructure (`/infrastructure`)

- `railway.toml` — Railway config as code (backend + frontend services)
- `docker-compose.dev.yml` — local PostgreSQL (port 5432) + test PostgreSQL (port 5433)

### Root

- `.gitignore` — covers Python, Node, IDE, env files

---

## 2. Files Created

| Path | Purpose |
|------|---------|
| `backend/app/main.py` | FastAPI app factory |
| `backend/app/config.py` | Settings via pydantic-settings |
| `backend/app/database.py` | Async SQLAlchemy engine + session |
| `backend/app/models/*.py` | All ORM models |
| `backend/app/schemas/*.py` | Pydantic v2 schemas |
| `backend/app/routers/*.py` | Route handlers |
| `backend/app/services/*/` | Service directory stubs |
| `backend/app/dependencies/` | DB + auth dependencies |
| `backend/tests/conftest.py` | Test fixtures |
| `backend/tests/test_api/test_health.py` | First test |
| `backend/pyproject.toml` | Python project config |
| `backend/requirements.txt` | Production dependencies |
| `backend/Dockerfile` | Railway backend container |
| `backend/.env.example` | Env variable template |
| `backend/alembic.ini` | Alembic config |
| `backend/migrations/env.py` | Async Alembic env |
| `backend/migrations/versions/0001_initial_schema.py` | Full initial schema |
| `frontend/package.json` | Node dependencies |
| `frontend/tsconfig.json` | TypeScript config |
| `frontend/next.config.ts` | Next.js config (standalone) |
| `frontend/tailwind.config.ts` | Design system config |
| `frontend/app/layout.tsx` | Root layout |
| `frontend/app/page.tsx` | Redirect to /dashboard |
| `frontend/app/*/page.tsx` | 6 page stubs |
| `frontend/components/providers.tsx` | React Query provider |
| `frontend/components/layout/sidebar.tsx` | Main navigation |
| `frontend/lib/utils.ts` | cn() utility |
| `frontend/lib/formatters/index.ts` | Amount + date formatters |
| `frontend/lib/api/client.ts` | Typed fetch client |
| `frontend/lib/api/companies.ts` | Companies API |
| `frontend/store/filters.ts` | Global filter state |
| `frontend/store/ui.ts` | UI state |
| `frontend/types/api.ts` | TypeScript API types |
| `frontend/Dockerfile` | Railway frontend container |
| `frontend/.env.example` | Frontend env template |
| `scripts/seed.py` | Company + taxonomy seed |
| `shared/constants.py` | Python constants |
| `shared/constants.ts` | TypeScript constants |
| `infrastructure/railway.toml` | Railway deployment config |
| `infrastructure/docker-compose.dev.yml` | Local dev database |
| `.gitignore` | Root gitignore |

---

## 3. How to Run Locally

```bash
# 1. Start local PostgreSQL
docker compose -f infrastructure/docker-compose.dev.yml up -d

# 2. Backend setup
cd backend
python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env                                # Edit if needed
alembic upgrade head                                 # Apply initial schema
python ../scripts/seed.py                           # Seed companies + taxonomy
uvicorn app.main:app --reload                       # Dev server on :8000

# 3. Frontend setup (new terminal)
cd frontend
npm install
cp .env.example .env.local
npm run dev                                          # Dev server on :3000

# 4. Visit http://localhost:3000
```

---

## 4. State at End of Session

**Done:**
- Full monorepo skeleton — both apps run (given a local PostgreSQL)
- Database schema complete — all 13 tables, all enums, all indexes
- Seed data ready — run `scripts/seed.py` once after schema migration
- Companies router live — `GET /api/v1/companies/` works against seeded data
- Health endpoint live — `GET /api/v1/health` with DB check
- Frontend renders with sidebar, routing, and placeholder pages

**Not started (next milestone):**
- No parser code (Milestone 1.2)
- No movement CRUD (Milestone 1.3)
- No classification rules engine (Milestone 1.4)
- No intercompany matcher (Milestone 1.5)
- shadcn/ui components not yet initialized (requires `npx shadcn-ui@latest init` in `/frontend` — do when first UI component is needed)

---

## 5. Blockers and Open Questions

None blocking Milestone 1.2. Open questions from TODO.md (G1, G3, G4, G6) are deferred and do not block the import engine.

---

## 6. Recommended Next Steps

**Milestone 1.2 — Bank Import Engine**

1. Implement parser base class (`backend/app/services/import_engine/parsers/base.py`)
2. Implement 12 bank-specific parsers following Spec 16 mappings
3. Implement auto-detection logic (`detector.py`)
4. Implement normalization pipeline (`normalizer.py`)
5. Implement deduplication (`deduplicator.py`)
6. Implement `ImportBatch` service + `POST /api/v1/imports` endpoint
7. Unit tests per parser (using actual sample files in `/samples/bank_statements/`)

Reference: `docs/specs/16_bank_format_normalization.md` — all 12 bank formats fully analyzed.

---

## 7. Quick Context for Next Session

Full skeleton is in place. Backend: FastAPI on `:8000`, async SQLAlchemy, all models and migrations done, seed script ready. Frontend: Next.js 14 App Router on `:3000`, Zustand + React Query configured, sidebar and routing working. Next work: `backend/app/services/import_engine/` — start with `base.py` parser class and Spec 16 as the reference.
